# Collision-toy feature: deliberately pinned to the SAME VRAM tilemap base as
# pin_a — the allocator must FAIL this build.
name = "pin_b"
role = "fixture"

[[claims.vram]]
name = "pin_b_map"
kind = "tilemap"
words = 0x400
at = 0x1000
