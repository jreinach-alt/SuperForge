; reg-gate fixture (FIRES): a scene write to a register NO feature of the
; scene claims — acceptance plant (b)'s committed shape.
.p816
s2_enter:
    sep #$20
    .a8
    lda #3
    sta a:$2106                 ; MOSAIC — nobody in this scene: FINDING
    rts
