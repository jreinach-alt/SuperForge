name: superforge

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: make patrol
        run: |
          make patrol
          size=$(stat -c%s build/patrol.sfc)
          echo "build/patrol.sfc = $size bytes"
<<<<<<< HEAD
          test "$size" -eq 524288 || { echo "::error::expected 524288 bytes, got $size"; exit 1; }

      - name: make maze
        run: |
          make maze
          size=$(stat -c%s build/maze.sfc)
          echo "build/maze.sfc = $size bytes"
=======
          test "$size" -eq 524288 || { echo "::error::expected 524288 bytes, got $size"; exit 1; }

      - name: make sprite_game
        run: |
          make sprite_game
          size=$(stat -c%s build/sprite_game.sfc)
          echo "build/sprite_game.sfc = $size bytes"
>>>>>>> claude/sweep-sprite_game
          test "$size" -eq 524288 || { echo "::error::expected 524288 bytes, got $size"; exit 1; }
