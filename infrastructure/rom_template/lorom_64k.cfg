# SuperForge 64KB LoROM Linker Configuration for ca65/ld65 (KIT-OWNED)
#
# Memory map for a 64KB LoROM SNES cartridge (2 banks).
# Used for ROMs that exceed the 32KB single-bank limit
# (e.g., the Mode 7 run-gate + racer, whose 32KB interleaved map
# blob fills BANK1).
#
# Bank layout:
#   Bank $00: Main code + data + ROM header + vectors (32KB)
#   Bank $01: Bulk data (tileset, tilemap, palette) (32KB)
#
# No SRAM region — see lorom.cfg for how to add one if your game
# needs battery saves.

MEMORY {
    # Zero page (Direct Page) — 256 bytes
    ZP:     start = $00,      size = $0100, type = rw, define = yes;

    # WRAM (CPU stack + general RAM)
    RAM:    start = $0100,    size = $1F00, type = rw, define = yes;

    # Bank 0: Main code ($00:8000-$00:FFBF, 32,192 bytes)
    ROM0:   start = $008000,  size = $7FC0, type = ro, file = %O, fill = yes, fillval = $00;

    # Internal ROM header ($00:FFC0-$00:FFDF, 32 bytes)
    ROMH:   start = $00FFC0,  size = $0020, type = ro, file = %O, fill = yes;

    # Interrupt vectors ($00:FFE0-$00:FFFF, 32 bytes)
    ROMV:   start = $00FFE0,  size = $0020, type = ro, file = %O, fill = yes;

    # Bank 1: Bulk data ($01:8000-$01:FFFF, 32KB)
    ROM1:   start = $018000,  size = $8000, type = ro, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    ZEROPAGE:   load = ZP,    type = zp;
    BSS:        load = RAM,   type = bss;
    CODE:       load = ROM0,  type = ro,  define = yes;
    RODATA:     load = ROM0,  type = ro;
    HEADER:     load = ROMH,  type = ro;
    VECTORS:    load = ROMV,  type = ro;
    BANK1:      load = ROM1,  type = ro,  define = yes;
}
