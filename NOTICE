# NOTICE — third-party components and provenance

## Vendored in this repository

### Brad Smith / rainwarrior — `dizworld` (`engine/mode7_hdma.asm`, `engine/mode7_math.asm` group-2 helpers, `engine/mode7_pv_ztable.inc`)
The Mode 7 perspective renderer is a derivative of `dizworld.s` from
https://github.com/bbbradsmith/SNES_stuff/tree/main/dizworld
(c) Brad Smith (rainwarrior), https://rainwarrior.ca — **Creative Commons
Attribution 4.0 International (CC BY 4.0)**, https://creativecommons.org/licenses/by/4.0/.
`engine/mode7_pv_ztable.inc` is the verbatim `pv_ztable` data block; the
`pv_*` HDMA routines in `engine/mode7_hdma.asm` and the group-2 math helpers
in `engine/mode7_math.asm` are modified transliterations (credit retained per
CC BY 4.0). Per-file notices are authoritative; consolidated attribution +
line-range correspondence in `docs/THIRDPARTY.md`.

### Terrific Audio Driver — ca65 API (`lib/tad/`)
Two files (`tad-audio.s`, `tad-audio.inc`) from
https://github.com/undisbeliever/terrific-audio-driver
(c) Marcus Rowe (undisbeliever) — **zlib license**, headers intact.
Vendored at upstream commit `822164b`. See `lib/tad/README.md`.

### Compiled TAD example song set (`assets/audio/tad_audio_data.{asm,bin}`, `tad_audio_enums.inc`)
Generated by `tad-compiler ca65-export` (the generated wrapper is
**Unlicense**, header intact) from the upstream Terrific Audio Driver
**example project** (`example-project.terrificaudio`: 24 demo songs,
19 sound effects, and their instrument samples). The blob also embeds the
TAD SPC700 loader + driver binaries (zlib, same upstream).
To ship your own music, rebuild this blob from your own TAD project with
`tad-compiler` — see `lib/macros/sf_audio.inc` for the build shape.

### CC0 art packs (`examples/itch_cc0/`)
Per-pack author, source URL, and license: `examples/itch_cc0/LICENSES.md`.
Converted derivatives under `templates/*/assets/` are CC0.

## Fetched on demand — never committed to this repository

- **Mesen2** (https://github.com/SourMesen/Mesen2) — GPLv3. `tools/setup.sh`
  builds `MesenCore.so` from source into `/tmp/Mesen2/` (and may cache it
  locally, git-ignored). The core is a development/test tool; it is not
  linked into, distributed with, or required by the ROMs this kit produces.
- **fullsnes** (https://problemkaputt.de/fullsnes.htm) — (c) Martin Korth,
  all rights reserved. Fetched by `tools/setup.sh` into the git-ignored
  `docs/reference/`; used locally as the authoritative hardware reference.
  If the canonical host is unreachable, setup falls back to the Internet
  Archive's snapshot of the same URL (same posture: fetched per-user,
  never committed). See `docs/reference/hardware/README.md`.
- **cc65** (https://cc65.github.io/) — installed via the system package
  manager by `tools/setup.sh`; not vendored.
