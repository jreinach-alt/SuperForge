# split_h_2p_demo — Audit Protocol audit-2 (remediation verification)

**Commits audited:** increment `088d0d1` + remediation `305c918` on `claude/persp-c-horiz-review-ntqlca` (HEAD = 305c918; `/home/user/SuperForge` tree left clean — research-only, nothing modified).
**Role:** independent AUDIT-2 — I did not build the increment, did not write audit-1, and did not write the remediation. Scope: verify the remediation commit against audit-1's five findings. All evidence below regenerated by me from a fresh materialization (`dryrun_split.sh /tmp/kit_audit2`), builds and measurements on the cycle-accurate emulator via MesenRunner.

## VERDICT: REMEDIATION VERIFIED

The remediation commit is exactly what it claims: the committed audit-1 report plus four targeted one-liner fixes, one per actionable finding, with finding 5 correctly left code-untouched (accept-as-is). `git show 305c918 --stat` = 4 files only: the new report (`asm_repo_staging/docs/audit/split_h_2p_demo-audit-1.md`, 81 lines), `main.asm` (comment-only — zero instruction changes, binary unaffected), `test_split_h_2p_demo.py` (the two test remediations and nothing else), `gen_pose_tables.py` (docstring-only — tool output unaffected, corroborated by the clean provenance gate). No scope creep. Post-remediation from scratch: builds clean (default + 4 variants), 16/16 tests, all four gates clean, and the widened latch window re-exercised three times with per-sample instrumentation — tears now land in 5 of 10 grabs (worst 6.53 vs deterministic clean 1.58; threshold 3.16), so the phase-intermittency audit-1 flagged (2 of 6) no longer has an all-clean-window failure mode under any observed phase, while discrimination is preserved (max-over-window cannot make a genuinely clean build exceed a deterministic baseline). One residual info nit noted under finding 2; it does not block.

## Per-finding disposition

| # | Audit-1 finding (all info-severity) | Remediation applied | Verified how |
|---|---|---|---|
| 1 | `gen_pose_tables.py` docstring claimed a "fit assertion warns"; no assertion exists | Docstring rewritten: tool "PRINTS the bank count; the budget itself is asserted by tests/test_gen_pose_tables.py, not here" | Read the code: `gen_pose_tables.py:108-110` computes banks and `print`s (no assert); `test_gen_pose_tables.py` `test_rotation_identities_and_bank_budget` asserts `banks == (2 if angles == 128 else 1)`. New wording matches reality exactly. Docstring-only; assets regenerate identically (provenance gate clean). |
| 2 | "Same value the HDMA delivers" exact only for band 1 — the only band measured; band 2 additionally value-stomped | Header comment (`main.asm:44-51`) rewritten: BAND-1's channel value, pure latch-interleave there, band 1 is the measured band, band-2 stomp explicitly called out as out of the metric window | Checked against code + test: the spin reads `HOFS1_MIR` ($C068, band-1 HOFS mirror = 384) and writes $210D; the test's jitter window is `_row_jitter(pix, 20, 100)` — scanlines 20-100, entirely inside band 1 (0-111). Comment now matches the measured reality audit-1 described. **Residual nit (info):** the inline comment inside the `.ifdef LATCH_VIOLATION` block (`main.asm:424-425`) still reads "the SAME value the HDMA channel delivers (... no value stomp)" — audit-1's citation included lines 420-436. True for the measured band; the primary documented model (header) is fixed. Not blocking. *(Disposition: fixed in the same commit that lands this report — inline comment aligned with the header wording.)* |
| 3 | Latch tear phase-intermittent (2 of 6 grabs) — potential flake if a window lands all-clean | Sample window widened `range(6)` → `range(10)` with an explanatory comment citing the audit-1 measurement | 3 independent `pytest -k latch` runs: 3/3 pass. My per-sample probe (3 fresh ROM-load cycles, exact test metric): tears in 5/10 grabs each cycle, per-sample `[6.53, 1.58, 1.58, 6.53, 5.42, 1.58, 1.58, 5.42, 6.53, 1.58]`, worst 6.53 = 2.07x the 3.16 threshold; longest clean streak observed = 2 grabs, so a 10-grab window cannot land all-clean under this cadence. Still discriminates: assertion is worst-over-window vs the deterministic frozen baseline (1.58 exactly), so widening adds sensitivity without adding false-pass or false-fail paths. |
| 4 | `test_m1` grabbed the reference screenshot twice under one tag (band refs from different frames, file overwrite) | Single `_grab` per build; both band signatures sliced from the same capture (`pix_a`/`pix_f`) | Diff + current tree (`test_split_h_2p_demo.py:245-246, 255-256`): one capture per tag, both band signatures from the same frame — strictly better semantics than before. Test passes in the suite run. |
| 5 | Latch variant's loop overruns the frame (inherent to the 2000-iteration spin, no 60 fps claim); "~40 stores/frame" figure conservative | Accept-as-is (documented in the committed audit-1 report); **no code change** — confirmed none was made | Full diff read: `main.asm` change is comments only; the spin (`ldx #2000` loop) and the "~40 CPU stores/frame" figure (`main.asm:54`) are untouched. The overrun and the conservative figure are on record in the committed report. Correct disposition — the overrun is the in-tree existence proof that the cadence metric detects a 2-frame loop, and the cadence gate measures the real cost. |

**Scope check:** the commit touches nothing beyond the report + the four fixes. The three source-file hunks map 1:1 onto findings 1-4; no drive-by edits, no baseline changes, no allocation/table/instruction changes.

## Evidence log (all regenerated by me from /tmp/kit_audit2 at 305c918)

```
$ bash asm_repo_staging/tools/dryrun_split.sh /tmp/kit_audit2
scrub_split: OK — 71 substitutions across 18 files; comment lineage guard clean
done — self-contained tree at: /tmp/kit_audit2

$ make split_h_2p_demo && bash templates/split_h_2p_demo/build_split_h_2p_variants.sh
built build/split_h_2p_demo.sfc (cfg=lorom_64k.cfg)
built build/split_h_2p_demo_freeze.sfc  (FREEZE=1)
built build/split_h_2p_demo_sameorigin.sfc  (FREEZE=1 SAME_ORIGIN=1)
built build/split_h_2p_demo_retarget.sfc  (FREEZE=1 RETARGET=1)
built build/split_h_2p_demo_latch.sfc  (FREEZE=1 LATCH_VIOLATION=1)

$ python -m pytest tests/test_split_h_2p_demo.py tests/test_gen_pose_tables.py -q
16 passed in 18.02s

$ bash tools/cleanroom_check.sh ; make width-check ; make zp-check ; make provenance-check
cleanroom: clean (name tripwire only — NOT a completeness guarantee; ...)
width-check: clean (189 files)
zp_lint: 0 finding(s) across 230 file(s); symbol table has 167 DP symbols covering 208 bytes
provenance: clean — 123 blob(s) accounted for (89 generated, 6 third-party, 9 attested, 19 artifact)
```

Remediated latch test, three independent runs:

```
$ for i in 1 2 3; do python -m pytest tests/test_split_h_2p_demo.py -k latch -q | tail -1; done
1 passed, 9 deselected in 2.35s
1 passed, 9 deselected in 2.34s
1 passed, 9 deselected in 2.33s
```

Per-sample probe (my script, exact test metric — `_row_jitter` over scanlines 20-100, threshold = 2.0 x max(clean, 0.5) = 3.16):

```
clean baseline jitter = 1.58   (frozen build — deterministic)
run 0: per-sample = [6.53, 1.58, 1.58, 6.53, 5.42, 1.58, 1.58, 5.42, 6.53, 1.58]  worst = 6.53  tear-grabs = 5/10  PASS
run 1: per-sample = [6.53, 1.58, 1.58, 6.53, 5.42, 1.58, 1.58, 5.42, 6.53, 1.58]  worst = 6.53  tear-grabs = 5/10  PASS
run 2: per-sample = [6.53, 1.58, 1.58, 6.53, 5.42, 1.58, 1.58, 5.42, 6.53, 1.58]  worst = 6.53  tear-grabs = 5/10  PASS
```

Cleanroom spot-check of the audit-1 report and remediation text themselves: the report materializes into the kit (`/tmp/kit_audit2/docs/audit/split_h_2p_demo-audit-1.md`) so the passing cleanroom gate already covers it; additionally I ran the gate's lineage and commercial-name denylist regexes, plus a supplemental sweep for vendor coprocessor/chip trade-name tokens not on the denylist, directly over the report file and the full `git show 305c918` output (message + diff): zero hits. Both documents report by mechanism only, and the report names no eliminated-lineage token.
