# split_h_2p_demo SPRITE STRESS RAIL — Audit Protocol audit-2

**Commit audited:** remediation `c500e346` on `task2-rail` (7th commit over base
`11791aa`; the 6 sprint commits `408a1a4..46cdd84` were audit-1's subject).
**Role:** independent AUDIT-2 — no part in the implementation, the trial, audit-1,
or the remediation. All evidence below regenerated by me this session from a fresh
materialization (`dryrun_split.sh /tmp/kit_a2t2`), default + all variants rebuilt,
every measurement on the cycle-accurate emulator via MesenRunner.
**Charter:** verify the remediation closes audit-1's findings — nothing more,
nothing broken. Audit-1's remediation scope was **finding 1 only** (MEDIUM,
test-hygiene: the glue test read the `$7E:E400` debug mirrors while the ROM
free-ran); findings 2–6 were accept/info.

## VERDICT: REMEDIATION VERIFIED — F1 closed at the mechanism, scope exactly as claimed, no regression

## 1. Scope verification of `c500e346` (the diff, read in full)

- **One file:** `git diff 46cdd845 c500e346 --name-only` →
  `asm_repo_staging/tests/test_split_h_2p_demo.py` only (`+26/−20`, one hunk,
  entirely inside `test_sp_glue_proof_pinned_and_forward_control`'s pinned
  read phase). No engine, template, generator, build-script, or doc change.
- **Token-level diff** (`--word-diff`): the ONLY token changes are
  `time.sleep(0.2)` removed and the 5-line PARK comment +
  `with runner.frame_stepping():` + `runner.frame_step(2)` added. Every other
  changed line is pure re-indentation into the `with` block (zero word-diff).
- **Assertion constants — explicitly compared pre/post, all identical:**
  render oracle `got >= 8` at `half=SP_TIER_HALF[tier]`; drift diagnostic
  `abs(msx − sx) <= 1 and abs(msy − sy) <= 1`; coverage floor `checked >= 6`;
  `SP_MIRBASE = 0xE400`; interior window `_sp_interior` untouched.
- **Pins unchanged:** `build_split_h_2p_variants.sh` diff = 0 lines; rebuilt
  this session — pin pairs still `(150,134) / (197,68) / (108,88)`
  (`SP_H1/SP_H2`, lines 80–82), `SPRITES=128 SP_MIR=1 SP_STATIC=1 FREEZE=1`.
- **Wrong-matrix control untouched:** the control section (post-remediation
  lines 1454–1475) has zero token changes — `spots >= 4`, `misses == spots`,
  the 24-px discrimination exclusion, and `half=7 / < 5` all identical. The
  control performs **no** mirror reads (its only WRAM reads are the pinned
  camera state for the two Python models), so F1 never applied to it;
  leaving it free-running is correct, not an omission.
- **F2 acknowledgment present** in the `c500e346` commit message, verbatim:
  "Finding 2 (LOW, ACCEPTED per audit-1 — no code change): SP_ALTFRAME's
  even-N contract remains comment-only; … a guard/round-up is due only if the
  probe is ever promoted to a shipping shape."

**Park pattern is real and is the suite's established idiom:** 13 other
`with runner.frame_stepping():` uses in the same file; the harness parks at
the canonical scanline 224 (end of the render period, just before the next
VBlank/NMI — `mesen_runner.py` `_CANONICAL_PARK_SCANLINE`). In the remediated
phase **every** read — camera state for `_sp_expected_main`, the screenshot,
and the mirror block — happens inside the block after the 2-frame settle. The
only read outside is the `$7E:E000` `"SFDB"` boot signature (static, outside
the mirror range).

## 2. Suite runs (verbatim, from /tmp/kit_a2t2)

Fixed test, 6 consecutive runs (remediation claimed 5/5; I ran 6):

```
=== RUN 1 ===  1 passed in 5.03s
=== RUN 2 ===  1 passed in 4.69s
=== RUN 3 ===  1 passed in 4.59s
=== RUN 4 ===  1 passed in 4.69s
=== RUN 5 ===  1 passed in 4.61s
=== RUN 6 ===  1 passed in 4.67s
```

Full 2p rail + tooling suite, then the split-family regression:

```
$ python -m pytest tests/test_split_h_2p_demo.py tests/test_gen_pose_tables.py -q
......................................                                   [100%]
38 passed in 89.41s (0:01:29)

$ python -m pytest tests/test_split_h_demo.py tests/test_split_h_matrix_demo.py \
    tests/test_split_h_persp_demo.py tests/test_split_h_persp3_demo.py \
    tests/test_split_v_demo.py tests/test_split_v_fight.py -q
..................x...................................                   [100%]
53 passed, 1 xfailed in 76.13s (0:01:16)
```

Both match audit-1's baselines exactly (38 = 27 rail + 11 tooling; 53 + 1 xfail).
No new test, no removed test, no behavior change outside the read phase.

## 3. Adversarial closure check on F1 (closed, not narrowed)

**(a) Whole-tree mirror-read sweep.** Grep of the entire `tests/` tree for the
mirror range (`0xE400–0xE7FF` literals, `SP_MIRBASE`, computed bases): exactly
**one** read site exists — `test_split_h_2p_demo.py:1446–1448` — and it is
inside the parked block, after the 2-frame settle. No other test file touches
the range. The race is closed at every reader, not just relocated.

**(b) Parked re-read stability (the brief's parking sanity check).** Probe
script against a fresh `spr_pin_a` boot, mimicking the remediated phase
exactly (boot → `frame_stepping()` → `frame_step(2)`), then reading the FULL
1,024-byte mirror block five times — at the settle and after four further
1-frame `frame_step` hops:

```
P: 5 parked snapshots (2-frame settle, then 1-frame hops) byte-identical: True
P: visible (non-sentinel) entries at park: 25 of 256; culled(sentinel): 231
P:   visible b0 i6: (286, 39) · b0 i20: (150, 52) · b0 i23: (235, 21) · …
```

The value reported at the park equals the parked re-read one frame later —
byte-identical across the whole block, all five snapshots. Reads at the park
are race-free.

**(c) Probe sensitivity control (is the race real and was it the park that
closed it?).** Same ROM, same block reads, but free-running — the PRE-fix
read pattern:

```
F: 600 free-running full-block reads in 0.1s: 44 caught a park-visible entry
   as $7FFF sentinel, 0 caught a non-sentinel value differing from park
```

44/600 free-running reads catch a stably-visible entry mid-rewrite as `$7FFF`
— audit-1's exact race, reproduced on demand outside the park and absent in
every parked read. The fix works because of the park, not by luck.

**Auditor's note (probe calibration, for future audits):** `$7FFF` is BOTH
the transient in-flight sentinel AND the persistent culled marker — 231 of
256 entries legitimately read `$7FFF` forever at these pins. A naive
"parked reads must be sentinel-free" probe false-alarms; the race signature
is a park-**visible** entry reading `$7FFF`. (Filed as a Task-2 paper cut.)
Related INFO nit, no action: the remediation's in-test comment says the loop
"rewrites every entry as sentinel-then-value each frame" — for culled entries
it is sentinel-only. The operative claim (a mid-rewrite read fakes a 32k-px
drift) is correct and is what the probe reproduces.

## 4. Gates ×4 (verbatim, from /tmp/kit_a2t2 WITH this report present at docs/audit/)

```
$ make width-check
width-check: clean (190 files)
$ make zp-check
zp_lint: 0 finding(s) across 231 file(s); symbol table has 167 DP symbols covering 208 bytes
$ bash tools/cleanroom_check.sh
cleanroom: clean (name tripwire only — NOT a completeness guarantee; see provenance_check.py + publish-time review)
$ make provenance-check
provenance: clean — 141 blob(s) accounted for (107 generated, 6 third-party, 9 attested, 19 artifact)
```

## 5. Verdict per finding

| Audit-1 finding | Disposition | Audit-2 verdict |
|---|---|---|
| **F1 (MEDIUM)** — glue test read the `$7E:E400` mirrors free-running; `$7FFF` sentinel caught live twice | Read phase parked via `frame_stepping()` + 2-frame settle; metric/thresholds/pins unchanged | **REMEDIATION VERIFIED** — sole mirror-read site in the tree is parked; parked block byte-stable across 5 snapshots; the race still fires 44/600 when deliberately read free-running, proving closure is mechanistic |
| **F2 (LOW)** — SP_ALTFRAME even-N contract comment-only | Accepted per audit-1; no code change | **VERIFIED AS ACKNOWLEDGED** — acknowledgment present in the `c500e346` commit message; code correctly untouched |
| **F3 (INFO)** — duplicated `sp_project_band` banner | No action required | Confirmed still present (`sprites_2p.inc:443–461`), correctly left alone — cosmetic |
| **F4 (INFO)** — deviations ledger not in tree | No action required | No change made; noted for future sprint hygiene |
| **F5 (INFO)** — ±255 clamp quantified | No action required | No change made |
| **F6 (INFO)** — overflow damage positional | No action required | No change made |

**Aggregate: REMEDIATION VERIFIED.** Scope exactly as claimed (one test's read
phase, one file, zero constant changes); the F1 race is closed at the
mechanism and at every reader; the fixed test is 6/6 green; both suites match
their baselines (38 · 53+1 xfail); all four gates clean with this report in
the tree. Nothing more, nothing broken.

*Audit artifacts (this session): probe script
`a2_mirror_probe2.py` (scratchpad; output quoted verbatim in §3), fresh
variant builds under `/tmp/kit_a2t2/build/`, glue screenshots regenerated by
the 6 test runs.*
